<p id="notice"><%= notice %></p>

<h1>Unit Numbers</h1>

<table class="center">
  <thead>
    <tr>
      <th>Officer</th>
      <th>Radio #</th>
      <th>Extra Info</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @radionumbers.each do |radionumber| %>
      <tr>
        <td><%= radionumber.officer %></td>
        <td><%= radionumber.radionumber %></td>
        <td><%= radionumber.extrainfo %></td>
        <td><%= link_to 'Show', radionumber %></td>
        <td><%= link_to 'Edit', edit_radionumber_path(radionumber) %></td>
        <td><%= link_to 'Destroy', radionumber, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Radio Number', new_radionumber_path %>
